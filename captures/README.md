# Captures

Ablage für Firmware-Mitschnitte. Vorgehen: siehe `docs/firmware-capture-playbook.md`.

- `.pcapng`/`.pcap` sind **gitignored** (lokal halten).
- `*__log.md` (Aktions-/Wert-Protokolle) kommen **in Git** — sie sind die Abgleich-Grundlage.
